{
  "name": "sample-oidc-client-app",
  "version": "0.1.0",
  "description": "A sample web service that authenticates using OIDC 1.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.3",
    "express-openid-connect": "^2.7.2",
    "mustache": "^4.2.0",
    "winston": "^3.6.0"
  }
}
